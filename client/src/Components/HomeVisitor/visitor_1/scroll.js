import ScrollReveal from 'scrollreveal';

const sr = ScrollReveal({ reset: true });
export const config = {
  origin: 'top',
  duration: 1500,
  delay: 150,
  distance: '300px',
  scale: 1,
  easing: 'ease',
};

export default sr;